export const TotalType = {
  total: Number,
  pageNumber: Number,
  pageSize: Number,
  refresh: Boolean
};

export const MailType = {
  configGroup: String,
  configID: Number,
  cropCode: String,
  recipients: String
};
